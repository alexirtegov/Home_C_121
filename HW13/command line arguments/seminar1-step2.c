/*
Этап 2: Базовая структура и ввод пути
Каждый раз, когда вы подключаете заголовок

#include <dirent.h>
вам становится доступно две «черные коробки»:

Тип DIR (реально это typedef struct __dirstream DIR;) — внутренняя структура, 
в которой libc хранит состояние «открытой папки» 
(файловый дескриптор, буфер для чтения записей, текущую позицию и т. д.).

Тип struct dirent — структура, в которую будут раскладываться отдельные записи каталога 
(имя файла, номер inode, тип и т. п.).

Вы вызываете
DIR *folder = opendir(full_path);
Например, если full_path равен строке ".", то внутри:
– libc сделает open(“.”, O_RDONLY | O_DIRECTORY), получит файловый дескриптор.
– через malloc или внутренний аллокатор выделит память под struct __dirstream (наш DIR).
– проинициализирует в ней поля:
• dd_fd — тот самый дескриптор,
• dd_loc = 0 и dd_size = 0 (пока буфер пуст),
• может выделить дополнительный буфер, куда будет читать «сырые» записи через 
системный вызов getdents.
– вернёт вам указатель folder на эту структуру.

«struct dirent *entry;»
– Здесь мы объявляем переменную entry.
– Она имеет тип «указатель на структуру dirent».
– Структура dirent описана в заголовке <dirent.h> (POSIX-совместимая). В ней, в частности, есть поле
     char d_name[256]; - это место, куда система помещает имя очередного файла или папки.  
 – Саму память под эту структуру мы не выделяем вручную. 
 Мы лишь говорим: «entry будет указывать на ту память, 
 которую мне даст система при каждом вызове readdir».
«while ((entry = readdir(folder))) { … }»
– Это главный цикл обхода. Функция readdir по POSIX-спецификации берёт указатель folder 
(его мы получили раньше из opendir) и возвращает:   

• Указатель на заполненную запись struct dirent — если в каталоге ещё остались файлы/папки.
• NULL — когда записи закончились или произошла ошибка.
– Выражение (entry = readdir(folder)) сначала выполняет присваивание, 
а затем возвращает само значение присвоения. Цикл while продолжится только пока entry не стал NULL.

«if (entry->d_name[0] == '.') continue;»
– Оператор -> используется для доступа к полям структуры через указатель.
– Здесь мы смотрим первый символ имени (d_name[0]). Если он точка ('.'), 
значит это либо «.» (текущая папка), либо «..» (родительская), либо файл, 
имя которого начинается с точки (на UNIX-платформах обычно это «скрытый» файл). 
Мы просто пропускаем такие записи оператором continue, возвращаемся 
к проверке условия цикла и дальше не печатаем их.


Тест 3: Текущая папка
Ввод: .
Ожидаемый результат: Программа выведет ваш абсолютный путь (например, Files in folder /home/user/my_c_project/:) и перечислит файлы внутри (включая скомпилированный файл программы и исходник .c). Скрытые файлы не выведутся.
Что проверяем: Срабатывает else if (dir[0] == '.'), вызывается getcwd.
Группа 3: Относительные пути (Вариант В)
Для этого теста создайте рядом с вашей программой папку test_folder и положите туда пару файлов.

Тест 4: Переход во вложенную папку
Ввод: test_folder
Ожидаемый результат: Программа склеит текущий путь и имя папки: Files in folder /home/user.../test_folder/. Выведет её содержимое.
Что проверяем: Срабатывает ветка else. Отрабатывает связка getcwd + добавление / + добавление имени папки strcat.
Группа 4: Обработка ошибок (Тестируем opendir)

Тест 5: Несуществующая папка
Ввод: /abracadabra
Ожидаемый результат: Unable to read directory: No such file or directory.
Что проверяем: Вызов opendir возвращает NULL. Программа корректно перехватывает ошибку, функция perror сама дописывает причину (No such file...) и делает return 1.

Тест 6: Нет прав доступа (если вы в Linux/macOS)
Ввод: /root (или любая папка, куда нет доступа обычному пользователю).
Ожидаемый результат: Unable to read directory: Permission denied.
Что проверяем: Снова возврат NULL, но функция perror показывает другую системную причину отказа.
🕵️‍♂️ Группа 5: Хитрые кейсы (Находим слабые места в коде)
Здесь мы проверим ограничения вашей текущей реализации. Попробуйте эти тесты, чтобы увидеть, как себя поведет код.

Тест 7: Папка с пробелом в имени
Ввод: My Documents
Результат: Программа скорее всего выдаст ошибку, что папка My не найдена.
Почему так вышло: Вы используете scanf("%255s", ...). Формат %s читает строку только до первого пробела. Слово Documents остается в буфере потока ввода. Если вам нужно поддержать пути с пробелами, вместо scanf лучше использовать fgets(dir, PATH_LENGTH, stdin); (но не забудьте удалить символ переноса строки \n в конце!).

*/

#include <stdio.h>   // Нужна для printf (вывод на экран) и scanf (чтение ввода)
#include <string.h>  // Инструменты для работы со строками (strlen, strcpy, strcat)
#include <unistd.h>  // Инструменты операционной системы (например, getcwd - где я нахожусь?)

#include <dirent.h> //даёт функции для работы с директориями: opendir, readdir, closedir, и тип struct dirent.

enum { PATH_LENGTH = 256 }; //создаёт в области компиляции целочисленную константу PATH_LENGTH, равную 256

//#define PATH_LENGTH 256 - просто текстом подставялется 256
#define STR255 "%255s"



void convert_path_to_full(char *full_path, const char *dir) {
	//В этой функции программа пытается угадать, что именно имел в виду пользователь, 
	//анализируя первый символ (dir[0]) того, что он ввел. Возможны три варианта.
	
    if (dir[0] == '/') {	//Вариант А: Идеальный пользователь (Ввел абсолютный путь)
        strcpy(full_path, dir);         //Пример: Пользователь ввел /user/docs.

		//Работа условия: dir[0] = '/' → условие истинно ✅ strcpy(full_path, "/user/docs")
		//Функция strcpy (string copy) берет всё, что лежит в dir ("/user/docs"), и перекладывает в full_path.


    } else if (dir[0] == '.') { //Вариант Б: Ленивый пользователь (Поставил точку)
        getcwd(full_path, PATH_LENGTH);  // Пример: Пользователь ввел . (обозначение текущей папки в Linux/Windows).
        //Логика: Программа понимает, что пользователь хочет путь к папке, в которой он прямо сейчас находится. 
        //Она вызывает getcwd ("get current working directory" — "дай ту папку, где я сейчас работаю"). 
        //Эта системная функция сама узнает полный путь (например, /home/user/projects) и кладет его в full_path.
        
    } else { //Вариант В: Обычный пользователь (Ввел имя папки)
		//Пример: Пользователь находится в /home/user и ввел просто Documents.
        getcwd(full_path, PATH_LENGTH);  // программа узнает, где мы: full_path становится равен "/home/user".
        strcat(full_path, "/");         //функция strcat (string concatenate, "склеить строки") приклеивает слэш в конец. full_path становится "/home/user/".
        strcat(full_path, dir);	//приклеиваем то, что ввел пользователь. full_path становится "/home/user/Documents".

    }
    // Добавляем / в конец если отсутствует
    if (full_path[strlen(full_path) - 1] != '/')
    //мы читаем full_path и считаем длину массива, сравниваем последний с '/' и если '/' нет, добавлем
        strcat(full_path, "/"); //добавляем '/' если его нет
}


int main() 
 {
	char dir[PATH_LENGTH];       // Создаем "коробку" для ввода пользователя (массив на 256 символов)
    char full_path[PATH_LENGTH]; // Создаем "коробку" для итогового результата (тоже на 256)
    printf("Input dir: "); 
    scanf(STR255, &dir[0]);  // Безопасный ввод (до 255 символов)
    
    convert_path_to_full(full_path, dir);
    printf("Files in folder %s:\n", full_path); //выведет только адрес рабочего каталога

    
    //В заголовке <dirent.h> (POSIX-стандарт) объявлен тип typedef struct __dirstream DIR;
    //где __dirstream – внутренняя реализация этой структуры.
    //То есть DIR — это через typedef переименованная внутренняя структура (обычно struct __dirstream)
    //DIR *folder это указатель на DIR
    DIR *folder = opendir(full_path); //открыть каталог с именем name (ваш full_path)
   //внутри себя выделяет (или инициализирует) область памяти под объект типа DIR, заполняет туда служебные поля
   //Если он вернул NULL, значит не получилось открыть папку 
   //(нет прав, нет такой папки и т. п.) 
   //и мы выводим системную ошибку через perror и завершаем программу.
   //«НЕ-NULL указатель» — указатель указывает на реальную область памяти, а не равен нулю.
   
      if (folder==NULL) {
        perror("Unable to read directory");
        return 1;
    }
    
    
    //следующая часть кода выдаст все файлы и папки, кроме скрытых.
    // она не будет заходить внутрь вложенных папок (то есть выведет только первый уровень).

    struct dirent *entry; //— это просто объявление указателя на ту самую структуру, описанную в <dirent.h>.
    int count = 0;
    while ((entry = readdir(folder))) {
		//Функция readdir(folder) просит операционную систему: "Дай мне следующий элемент из папки".
		//и первом вызове она выдает 1-й файл. При втором — 2-й.....
        // Пропускаем скрытые файлы и директории (начинающиеся с '.')
        //Когда файлы в папке заканчиваются, readdir возвращает NULL (пустоту/ноль). Цикл while видит этот ноль и завершает свою работу.
        
        if (entry->d_name[0] == '.') //Эта строка работает как фильтр.Здесь проверяется нулевой символ имени файла (первая буква).
        //Если имя файла начинается с точки (.), то вызывается continue — это команда "пропустить всё, что ниже, и перейти к следующему шагу цикла".
        //Зачем это нужно? в Linux скрытые файлы начинаются с точки. Во-вторых, в любой папке всегда есть две невидимые системные ссылки: . (указывает на саму эту папку) и .. (указывает на папку на уровень выше).
            continue;
        printf("%s\n", entry->d_name); //здесь мы печатаем все файлы
        count++;
        
    }
    printf("Total: %d files\n", count);
    closedir(folder);
    return 0;
}
