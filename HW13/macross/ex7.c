/*

 */


#include <stdio.h>

//Операция склейки записывается как <arg1>##<arg2>
#define MERGE(a,b) a##b

int main(void)
{
    MERGE(d,o) // do
{
    int MERGE(a,2); // int a2
    a2 MERGE(+,+); // a2++
}while(1);

    return 0;
}
